<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <title>Live Weather App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1"/>
    <meta name="description" content=""/>
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="icon" href="/assets/icon.png">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
          rel="stylesheet">
</head>
<body>
<div class="main-div">
    <h1>Live Weather App</h1>
    <div id="form-div" <% if(locals.wide) {%> class="wide" <% }%>>
        <form action="/" method="POST">
            <input class="input" type="text" name="city" placeholder="City name">
            <input id="submitBtn" type="submit" value="Search">
        </form>
    </div>
    <% if(locals.error) {%>
    <div>
        <h2>City not Found</h2>
    </div>
    <% } if(locals.data) {%>
    <div class="weather-widget">
        <div id="left">
            <div>
                <p id="location-text"> <%= data.location.name %>, <%= data.location.country %></p>
                <p id="degree-text"><%= Math.round(data.current.temp_c) %>°</p>
            </div>
            <div id="feels-like-div">
                <p id="status-text"><%= data.current.condition.text %></p>
                <p id="feels-like-text">Feels like <%= Math.round(data.current.feelslike_c) %>°</p>
            </div>
        </div>
        <div id="right">
            <div id="img-div">
                <img src="<%= data.current.condition.icon %>">
            </div>
            <div id="stats-div">
                <p id="humadity">Humidity: <%= data.current.humidity %>%</p>
                <p id="wind">Wind: <%= data.current.wind_kph %> km/h</p>
            </div>
        </div>
    </div>
    <% } %>
</div>
<script src="/index.js"></script>
</body>
</html>